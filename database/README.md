# Database

This project currently uses in-memory simulation state managed by the backend execution engine. No external database is required for the current application.

This folder is reserved for future database schemas, migrations, and persistence adapters.
